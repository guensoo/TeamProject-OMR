import { Text, View,StyleSheet} from 'react-native';

export const Navbar = () => {

    const styles = StyleSheet.create({
        container:{
            
        }
    })

    return(
        
    )
}
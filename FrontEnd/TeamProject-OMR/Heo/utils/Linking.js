const linking = {
    prefixes: ['myapp://'],
    config: {
        screens: {
            ResetPassword: 'reset-password',
            // 필요하면 다른 스크린도 여기에 추가
        },
    },
}

export default linking;